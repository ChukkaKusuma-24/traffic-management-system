{
  "name": "traffic-management-system",
  "private": true,
  "version": "1.0.0",
  "description": "Government-grade smart city traffic control platform",
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -n backend,frontend -c yellow,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend run start",
    "start:prod": "npm --prefix backend run start:prod",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "db:migrate": "npm --prefix backend run prisma:migrate",
    "db:deploy": "npm --prefix backend run prisma:deploy",
    "db:seed": "npm --prefix backend run prisma:seed"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
